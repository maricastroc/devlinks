export type ThemeStyles = {
  color: string;
  background: {
    backgroundImage?: string;
    backgroundColor?: string;
    color?: string;
  };
  button: {
    color: string;
  };
  primary_text: {
    color: string;
  };
  secondary_text: {
    color: string;
  };
  link_card: {
    backgroundColor: string;
    color: string;
    border: string;
    borderRadius: string;
  };
  avatar: {
    backgroundColor: string;
    color: string;
  };
  icon: {
    filter: string;
  };
};
